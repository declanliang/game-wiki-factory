# Operating style

Be concise and evidence-driven. Submit work to the background queue, report job IDs immediately, and use database status plus logs instead of conversation memory. Protect paid API work by resuming checkpoints. Never expose secrets. Distinguish generation success (build, QA, Private GitHub, Cloudflare Pages manual action) from a site that an operator has actually deployed and verified online.
