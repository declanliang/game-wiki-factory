# 游戏基础配置自动化验收报告

验收日期：2026-07-17

## 1. 验收范围

使用最终代码，从游戏名开始完整执行：

1. Roblox Discover 身份发现；
2. Place→Universe 与官方事实采集；
3. 联网 LLM 外部资源研究（本报告中的原始双样本由 OpenRouter 生成；当前运行链已迁移到 ToAPIs Responses）；
4. 首页配置生成；
5. 4–8 个首页模块生成；
6. Hero 图片下载与 favicon 图标包转换；
7. Schema、来源、URL 和业务规则验证。

## 2. 最终结果

|样本|Place ID|Universe ID|状态|模块|Hero 图|主要结论|
|---|---:|---:|---|---:|---:|---|
|Anime Expeditions|84515722934860|7613921865|warning|6|2|身份与结构正确；3 个代码只有第三方证据，保持 Unverified|
|PursuitCore|121903154323395|10428543942|pass|6|1|正确识别当前 Place，并从官方 Linktree 排除旧 Place 84498985865861|

两者均满足：

- Roblox 身份准确；
- JSON Schema 错误为 0；
- Hero stats 恰好 5 条；
- Start cards 恰好 4 张；
- 模块数量在 4–8；
- SEO description 为 140–160 字符；
- favicon 包完整；
- 前台 references 没有竞对 Wiki/Fandom/wiki.gg/fextralife；
- 没有把第三方代码标记为官方 Active。
- `game-wiki-template` 专用 `site-identity.json + site-content.json` 双文件强契约通过；
- Hero stats 已转换为模板要求的 4 个 `{value,label}` 对象；
- FAQ 已补齐为 5 条，问题均包含游戏名；
- `site-content.json` 顶层只有 `site/home`；旧版 themeColor/sidebarCodes/start/modules 字段不会进入模板输入；
- Hero 自动字段只由 `site-identity.json` 驱动，不会被内容文件覆盖。

模板字段映射与真实 `apply-content.mjs` 冒烟结果见 `docs/testing/模板首页契约兼容性.md`。

## 3. 最终版首次运行实测

|样本|费用|总耗时|说明|
|---|---:|---:|---|
|Anime Expeditions|$0.744911|184.29 秒|6 模块，第三方代码触发 warning|
|PursuitCore|约 $0.75|约 136 秒|复杂旧 Place 消歧样本，最终 pass|

以上是迁移前 OpenRouter 历史费用，不代表 ToAPIs 定价。价格会随模型搜索次数、模型和网关路由变化。

## 4. 缓存验收

PursuitCore 在事实未变化时重复运行：

```text
status: pass
external_research: cached=true
homepage_config: cached=true
homepage_modules: cached=true
run cost: $0
total time: 4.68 seconds
```

这证明动态采集时间和证据时间戳不会造成模型缓存误失效。

## 5. 自动化测试

执行命令：

```powershell
python -m compileall -q src tests
python -m unittest discover -s tests -v
```

最终结果：16 项测试全部通过，0 项失败、0 项跳过（包含 ToAPIs Responses 解析、未知费用处理、双文件模板契约和失败文件阻断测试）。

另使用模板仓库当前 `check-intake.mjs + apply-content.mjs` 完成隔离消费者冒烟测试，0 error、0 warning。

## 6. 交付判定

第一版“输入 Roblox 游戏名 → 输出首页建站配置”的目标已经完成，可以开始接入通用网站模板。

`warning` 并不表示程序失败，而是拒绝掩盖资料本身的不确定性。Anime Expeditions 的兑换码应由人工确认或等待官方来源后再升级为 Active。
